{
  "name": "@rlm/pi-python-kernel",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "keywords": ["pi-package"],
  "description": "Delegation and the agent-side seams for pi, inside the code-mode Python kernel",
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "peerDependencies": {
    "pi-code-mode": "*",
    "pi-tool-bridge": "*"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
